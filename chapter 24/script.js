/* Strings:
Finding a character at a location */

var str = "saylani";
console.log(str.charAt(2)); // select index of 2 y

var lastChar = str.charAt(str.length - 1);
console.log(lastChar);  // -1 is i 